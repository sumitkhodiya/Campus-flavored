services:
  - type: web
    name: campus-flavored-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: WHATSAPP_VERIFY_TOKEN
        value: campusflavored123
      - key: JWT_SECRET
        value: super_secret_jwt_key_campusflavored_2026
